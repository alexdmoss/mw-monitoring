---
apiVersion: v1
kind: Service
metadata:
  labels:
    prometheus: mw
    app: prometheus
  name: prometheus
  namespace: ${NAMESPACE}
spec:
  type: ClusterIP
  ports:
    - name: web
      port: 9090
      targetPort: web
  selector:
    prometheus: mw
